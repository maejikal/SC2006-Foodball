## 2006-SCSB-35 

# SETUP
npm install react-router-dom vite node
pip install -r requirements.txt

vite to build and run, served at http://localhost:5173/
need to run both vite and flask at the same time
cd 'C:\Program Files\MongoDB\Server\8.2\bin'
mongod --port 27017 --dbpath "D:\NTU\SC2006\2006-SCSB-35\2ManyFoods\db" (replace this with the path to the db folder) 